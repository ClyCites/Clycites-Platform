# Docker infrastructure

The root Compose file currently contains all local-service configuration. Add container-specific
configuration here only when inline Compose configuration is no longer maintainable.
